# NOTE: This file contains placeholder base64-encoded values for demo purposes only.
# In production, use a proper secret manager (Vault, AWS Secrets Manager, Sealed Secrets, etc.)
# and never commit real credentials to source control.
#
# To generate a value: echo -n 'your_value' | base64
#
# Example decodes:
#   DB_USERNAME -> order_user
#   DB_PASSWORD -> order_pass
apiVersion: v1
kind: Secret
metadata:
  name: order-app-secret
  namespace: default
  labels:
    app: order-management
type: Opaque
data:
  DB_USERNAME: b3JkZXJfdXNlcg==
  DB_PASSWORD: b3JkZXJfcGFzcw==
